evaluation task: columns (top of the column head)
2nd row of the column head contains the metrics.
If multiple metrics are given per column, extract each metric name and metric value separately (one entry each).